homemade text editor in C using this tutorial: 
https://viewsourcecode.org/snaptoken/kilo/
======================================================
heavily commented because I am learning, not intended to be actually used but will probably use it for a project at some point. I will be adding features as I learn more about C, so expect it to be a bit of a mess for a while. I will also be adding more comments as I go along, so expect those to be a bit of a mess as well.